{{define "title"}}Home{{end}}

{{define "main"}}
<div class='snippet'>
    <!-- <div class='metadata'>
        <strong>{{.Post.Title}}</strong>
        <span>#{{.Post.ID}}</span>
    </div>
    <pre><code>{{.Post.Text}}</code></pre>
    <div class='metadata'>
        <time>Created: {{.Post.Created}}</time>
    </div> -->

    <!-- Hardcoded comment section -->
    <div class='comments'>
        <h3>Kategooriad</h3>
        <ul>
            {{range .Categories}}
            <!-- Nüüd võtab sealt -->
            <li>
                <!-- <strong>{{.Name}}</strong>  -->
                <!-- <p>{{.ID}}</p> -->
                {{.ID}} <strong><a href="/category/view/{{.ID}}">{{.Name}}</a></strong>
            </li>
        
            {{else}}
            <!-- Kui kommentaare ei ole DB-s siis näeme seda -->
            <p>There's nothing to see here... yet! Come back soon!</p>
            {{end}}
        </ul>
    </div>
</div>
{{end}}